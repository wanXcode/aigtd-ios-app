# AIGTD Reminders v0.7.0

## 版本信息

- 版本：`0.7.0 (24)`
- 状态：TestFlight 内部候选
- 主题：公开产品体验升级
- 升级基线：`0.6.0 (20)`

## 已完成更新

- 默认事务助理统一为“小满”，首次进入 Chat 提供一次简短欢迎。
- 公开导航收敛为 `AIGTD` 与 `任务` 两个 Tab。
- 重构 Chat 输入框、键盘联动、历史阅读、流式回复和卡片状态过渡。
- 输入框使用“发消息或按住说话…”入口，支持长按录音、上滑取消和松手转可编辑草稿。
- 普通聊天不显示执行卡片；任务卡片在同一位置从待确认更新到真实结果。
- 任务页改为区别于 Apple Reminders 的只读 AIGTD 任务概览。
- 清单顺序尽量跟随系统并显示空清单；不再擅自按任务状态、时间和标题重排。
- Release 产品隐藏模型、接口、密钥、Prompt 和诊断等开发者配置。
- 统一暖白、品牌蓝和小满杏橙视觉，补齐 Dynamic Type、VoiceOver 与 Reduce Motion。
- 修复 Build 21 因 CocoaPods 动态框架未嵌入安装包而导致的启动即崩溃。
- 修复 Build 22 中 EventKit 后台回调直接进入 `AppModel` 主 actor、触发 Swift 运行时隔离断言的启动崩溃。
- `xcodegen generate` 后自动执行 `pod install`，并新增归档动态依赖门禁，避免同类发布事故复发。

## 验收状态

- 自动化测试：376 个 XCTest 方法已在 iPhone 15 Pro Max 真机执行，0 失败；语音专项 12 项全部通过
- Debug 测试构建：generic iOS Simulator 与 generic iOS 均通过 `build-for-testing`
- Release 验证：generic iOS Release 构建和静态分析通过
- 代码与产物检查：`git diff --check`、Release 开发者文案扫描和敏感资源扫描通过
- 真机启动回归：Build 23 开发签名包已在 iPhone 15 Pro Max 启动，完成首次 Reminders 刷新后持续运行，且未产生新崩溃日志
- 真机功能回归：待执行
- Chat 性能检查：待执行
- 无障碍检查：待执行
- Archive：已成功生成 `AIGTDReminders-0.7.0-23.xcarchive`，企业 Team、Bundle ID、版本、arm64 元数据和动态框架依赖核对通过
- TestFlight 上传：2026-08-03 已成功上传 Chat 输入与语音体验修复构建 `0.7.0 (24)`，Xcode 返回 `Upload succeeded` 和 `EXPORT SUCCEEDED`，当前处理中；Build 21 与 Build 22 均不再作为验收候选
- 覆盖升级：待执行

## Build 21 启动崩溃结论

- 表现：从 TestFlight 安装后，点击 AIGTD 立即被系统终止，尚未进入 SwiftUI 启动逻辑。
- 根因：主程序链接了 `@rpath/TTNetworkManager.framework/TTNetworkManager`，但 Build 21 的 App 包没有 `Frameworks` 目录；`xcodegen` 重建工程后遗漏了 CocoaPods 的 `[CP] Embed Pods Frameworks` 阶段。
- 证据：Build 21 归档的依赖门禁稳定报出 `missing embedded dependency`；Build 22 的 Release App 与正式 Archive 均通过同一门禁，且框架为 arm64。
- 处理：Build 21 不作为可验收候选；Build 22 继续用于确认框架修复，但不再发布给测试者。

## Build 22 启动崩溃结论

- 表现：动态框架已能正常加载，应用进入 SwiftUI 并开始读取系统 Reminders 后被系统终止。
- 根因：`AppModel` 标记为 `@MainActor`，其 EventKit `fetchReminders` 完成闭包却由 `com.apple.eventkit.reminders.search` 后台队列调用；Swift 6 运行时检测到错误的 executor 后触发 `EXC_BREAKPOINT / SIGTRAP`。
- 证据：设备日志的故障线程为 EventKit 搜索队列，符号化后的 App 栈定位到 `AppModel.fetchReminderItemsInSystemOrder()` 的完成闭包。
- 处理：EventKit 获取与数据转换移入 `nonisolated` 边界，仅将 `Sendable` 的 `ReminderItemInfo` 返回主 actor 更新 UI；Build 23 已通过真机实际启动和崩溃目录复查。

## 安全与升级说明

- `0.7.0` 安装包不再包含 `Secrets.local.plist`、示例密钥文件或面向开发者的模型配置入口。
- 本轮 TestFlight 应在 `0.6.0 (20)` 上直接覆盖安装，以保留设备上已有的本地模型和语音配置；不要先卸载旧版。
- 全新安装在服务端 AIGTD 模型与语音代理上线前不会自动获得 AI 凭证，因此暂不作为公开 App Store 的新用户交付方式。
- `0.6.0` 曾使用过的客户端凭证须在正式公开发布前轮换，公开版只允许通过服务端代理访问模型与语音服务。
- EventKit 不提供可靠的手工排序接口；任务页优先采用系统返回顺序，并以会话内稳定顺序作为可解释降级，不自行按标题、时间或完成状态重排。

## 发布条件

以 [`v0.7.0 产品体验升级 PRD`](v0.7.0-prd.md) 的 36 项验收场景为准。完成 TestFlight 覆盖升级与人工体验验收后，才将本文件状态改为已发布。
